__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
.DS_Store
data/
.env
# Any .env sidecar (.env.bak.*, .env.local, .env.anthropic, ...) may hold live
# credentials — ignore the whole family and allow only the checked-in template.
.env.*
!.env.example
src/cycle_sim/
replays/
*.db
.claude/

# Mullvad WireGuard configs — contain private keys, never commit
mullvad_wireguard_linux_all_all.zip
*.conf
mullvad-wg/
