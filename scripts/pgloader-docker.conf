/*
 * pgloader config — MariaDB → PostgreSQL migration.
 * Step 1: Migrate as text, then ALTER to jsonb post-migration.
 */

LOAD DATABASE
    FROM mysql://clash_stats:clash_stats_pw_2026@clash-mariadb:3306/clash_stats
    INTO postgresql://clash_stats:clash_stats_pw_2026@clash-postgres:5432/clash_stats

WITH include drop, create tables, create indexes, reset sequences,
     workers = 2, concurrency = 1,
     prefetch rows = 1000, rows per range = 5000

ALTER SCHEMA 'clash_stats' RENAME TO 'public'
;
